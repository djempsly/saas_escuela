# ============================================================
# Variables de Entorno - Backend
# Copia este archivo como .env y ajusta los valores
# ============================================================

# ============ Base de datos ============
DATABASE_URL=postgresql://usuario:password@localhost:5432/plataforma_escolar

# ============ Seguridad ============
JWT_SECRET=cambiar-esto-por-una-clave-secreta-de-al-menos-32-caracteres

# ============ Servidor ============
NODE_ENV=development
PORT=4000
FRONTEND_URL=http://localhost:3000

# ============ CORS ============
# Orígenes permitidos separados por coma (en producción)
# ALLOWED_ORIGINS=https://midominio.com,https://admin.midominio.com

# ============ Multi-tenant ============
# BASE_DOMAIN=midominio.com
# SERVER_IP=123.456.789.0

# ============ AWS S3 (almacenamiento de archivos) ============
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=tu-access-key-id
AWS_SECRET_ACCESS_KEY=tu-secret-access-key
AWS_S3_BUCKET=mi-bucket-escolar
AWS_CLOUDFRONT_URL=https://d1234567890.cloudfront.net

# ============ Redis ============
REDIS_URL=redis://localhost:6379

# ============ Sentry (monitoreo de errores, opcional) ============
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# SENTRY_TRACES_SAMPLE_RATE=0.1

# ============ Email SMTP (opcional) ============
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=correo@ejemplo.com
# SMTP_PASS=tu-password-de-aplicacion
# SMTP_FROM=noreply@ejemplo.com

# ============ Docker Compose (solo para docker-compose.yml) ============
# DB_USER=postgres
# DB_PASSWORD=password-seguro
# DB_NAME=plataforma_escolar
