# ==============================================================
# Variables de entorno - Plataforma Escuela v2 (Backend)
# Copia este archivo como .env y completa los valores.
# ==============================================================

# ---- Base de datos (requerido) ----
DATABASE_URL="postgresql://usuario:password@localhost:5432/educativa"

# ---- Servidor ----
PORT=4000
NODE_ENV=development

# ---- Seguridad (requerido, min 32 caracteres) ----
JWT_SECRET="cambiar-en-produccion-min-32-caracteres-seguro"

# ---- Frontend URL ----
FRONTEND_URL=http://localhost:5173

# ---- CORS (separar orígenes por coma) ----
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ---- Multi-tenant (requerido en producción) ----
# BASE_DOMAIN=tudominio.com
# SERVER_IP=123.456.789.0

# ---- AWS S3 / CloudFront (almacenamiento de archivos) ----
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_BUCKET_NAME=
# CLOUDFRONT_URL=https://xxxxxx.cloudfront.net

# ---- Sentry (monitoreo de errores) ----
# DSN del proyecto en Sentry. Si está vacío, Sentry queda deshabilitado.
# Se obtiene en: Sentry > Settings > Projects > [tu proyecto] > Client Keys (DSN)
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# Porcentaje de transacciones que se rastrean para performance (0.0 a 1.0).
# 0.1 = 10% de las requests. En producción, usar 0.1 o menos para no saturar la cuota.
# En desarrollo se puede subir a 1.0 para ver todas las trazas.
# SENTRY_TRACES_SAMPLE_RATE=0.1

# ---- Email / SMTP (opcional - si no está, forgot-password no envía emails) ----
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu-email@gmail.com
# SMTP_PASS=tu-app-password
# SMTP_FROM=noreply@tudominio.com
