# =============================================================
# Variables de Entorno - Producción
#
# IMPORTANTE: Copiar este archivo a .env y cambiar TODOS los valores
# No subir el archivo .env al repositorio
# =============================================================

# ============ Base de Datos ============
DB_USER=plataforma_user
DB_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURO_MINIMO_32_CARACTERES
DB_NAME=plataforma_educativa

# ============ Seguridad ============
# Generar con: openssl rand -base64 48
JWT_SECRET=CAMBIAR_POR_STRING_DE_MINIMO_64_CARACTERES_ALEATORIOS

# ============ Dominio ============
# Tu dominio base (ej: lhams.com, miescuela.com)
BASE_DOMAIN=tudominio.com
# IP pública de tu servidor
SERVER_IP=0.0.0.0
# URL del frontend
FRONTEND_URL=https://tudominio.com

# ============ Aplicación ============
NODE_ENV=production
PORT=4000

# ============ CORS ============
# Orígenes permitidos (separados por coma)
ALLOWED_ORIGINS=https://tudominio.com,https://app.tudominio.com

# ============ Email (Opcional) ============
# Si no se configura, forgot password no enviará emails
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=tu_usuario
# SMTP_PASS=tu_password
# SMTP_FROM=noreply@tudominio.com
